services:
  # Disable Ollama in CI - not needed when RCA_PROVIDER=rules
  ollama:
    profiles: ["manual"]

  phoenix-controller:
    environment:
      RCA_PROVIDER: rules
    depends_on:
      prometheus:
        condition: service_healthy
